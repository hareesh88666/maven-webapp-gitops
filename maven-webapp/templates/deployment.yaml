apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "maven-webapp.fullname" . }}
  labels:
    app: {{ include "maven-webapp.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "maven-webapp.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "maven-webapp.name" . }}
    spec:
      containers:
        - name: {{ include "maven-webapp.name" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8080

